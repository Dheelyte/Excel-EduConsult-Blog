# Excel EduConsult Blog
### To set up the project locally after pulling, run the following commands:

- ```cd ExcelEduConsult```
- ```pip install -r requirements.txt```
- ```python manage.py migrate```

### To run the project:
- ```python manage.py runserver```